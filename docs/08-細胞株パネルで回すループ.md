# 細胞株パネルで回すループ

提出を待たずに候補を判定するための、公開データだけで作った検証パネル。
`06`（採点コードを手元で回す）と `07`（予算の較正）の続き。

---

## 13. なぜ必要か

提出は 1 日 2 回、返ってくるのは context ごとの 1 数字だけ。
`v12` が +0.01745、その前の `v11` が +0.00855 で、いま試したい変更の効き幅は
**+0.005 前後**。300 摂動のばらつきの中では、1 回の提出でそれが本物かノイズか
区別できない。しかも 1 回試すのに半日かかる。

手元の H1 リファレンス（60 摂動）1 本では、同じ理由でやはり足りない。
**細胞株を増やして符号を見る**のが、この規模の差に対して使える唯一の道具。

## 13a. パネルの作り

`scripts/panel_score.py`。1 細胞株につき

* **対照細胞** — 有意化しきい値と propensity はここからしか作らない
  （本番でも test context の対照細胞は配られる）
* **`cell_eval2` で計算した DE 表** — 採点の真値

を持ち、予測に使う信号は**その株以外**からだけ当てる。H1 を採点するときは
ライブラリの `H1` / `H1val` / `H1test` を全部落とす（接頭辞一致で落とす）。

| 株 | 出所 | 摂動 | 細胞/摂動 | 遺伝子 | 平均 `n_real` |
|---|---|---|---|---|---|
| H1 | 2025 VCC 訓練データ | 60 | 400 | 18,080 | 1,028 |
| K562 | Replogle（scPerturb） | 300 | 100 | 8,563 | 254 |
| RPE1 | Replogle（scPerturb） | 300 | 100 | 8,749 | 462 |
| （本番 2026 val） | — | 300 | 400 | 10,773 | **≈275** |

公開データの制約は固い。**Replogle で 400 細胞以上ある標的は K562 で 64、
RPE1 で 37 しかない**ので、「300 標的 × 400 細胞」という本番と同じ形は作れない。
そこで形を崩して標的数を優先した。結果として起きたことは皮肉で、
**形の正しい H1 より、形を崩した K562 のほうが本番の強さに近い**
（`n_real` 254 対 1,028、本番 ≈275）。

## 13b. 何が比較できて、何ができないか

* **できる**: 2 つの候補を同じ株で採点したときの**差の符号**。
  3 株で符号が揃えば、その変更は細胞株を跨いで効く。
* **できない**: 株をまたいだ絶対値の比較。H1 の摂動は本番の 4 倍近く強く、
  K562/RPE1 は細胞数が 1/4 なので、`fid` も `jac` も水準がそもそも違う。
* **できない**: 排出に依存する量（margin、ランプ、疑似カウント）の判定。
  K562/RPE1 は 100 細胞/摂動しか無いので Wilcoxon の検出力が足りず、
  K=3,000 を宣言しても**実際に有意になるのは中央値 21 遺伝子**で、
  `cell_eval2` は `fid` と `reach` に `nan` を返す。
  排出まで含めた判定は 400 細胞/摂動の H1 リファレンスでしかできない（`07` §9c）。

`pds` だけは代理指標である点にも注意。本物の `pds` は pseudobulk プロファイルを
順位付けするが、ここでは予測 log2FC と真の log2FC のコサイン順位を使っている。

**自分の標的遺伝子は両側でゼロにしてから**でないと意味を持たない。
真値では knockdown 自身が最大の成分で、予測は予算をそこに使わないので、
残差プロファイルが予測とは無関係な理由で逆を向く（マスク前 0.19、マスク後 0.54）。
採点側の全メンバーが同じ除外をしている。

## 13c. 近傍項は 3 株すべてで同じ符号

`12` で測った CRISPRi の近傍効果を、この 3 株で当て直した
（信号は generic 応答のみ、K=8,000、他の 2 株から転移）:

| 重み | 株 | `pds` | `fid` | `reach` | `jac` | `nmae` |
|---|---|---|---|---|---|---|
| ×5 | H1 | +0.0528 | ±0 | +0.0056 | ±0 | −0.0013 |
| | K562 | +0.0429 | ±0 | +0.0063 | ±0 | −0.0005 |
| | RPE1 | +0.0200 | ±0 | +0.0053 | ±0 | −0.0009 |
| ×20 | H1 | +0.0661 | ±0 | +0.0049 | ±0 | −0.0013 |
| | K562 | +0.0394 | ±0 | +0.0067 | ±0 | −0.0005 |
| | RPE1 | +0.0182 | ±0 | +0.0070 | ±0 | −0.0008 |
| ×50 | H1 | +0.0642 | ±0 | +0.0072 | ±0 | −0.0014 |
| | K562 | +0.0417 | ±0 | +0.0066 | ±0 | −0.0005 |
| | RPE1 | +0.0168 | ±0 | +0.0070 | ±0 | −0.0008 |

**`pds`・`reach`・`nmae` の符号が 3 株すべてで揃う。** `fid` と `jac` は
そもそも動かない（呼び出し集合の中身がほとんど変わらないため）。
重みは 5〜50 で平坦なので、v14 の 20× は峰ではなく台地の上。

overall への寄与に直すと、本番に近い 2 株で **+0.008〜+0.015**。
v12 の +0.01745 に対して小さくない。v14 の実測がこの予測の答え合わせになる。

## 13d. 予算 K=1,000 は取り下げ

`07` §9c は排出込みで K=3,000 を最良としたが、K=1,000 が本当に悪いのかは
提出して確かめる予定だった。パネルは 3 株すべてで**悪い**と答えた:

| K | K562 | RPE1 | H1 | K562+RPE1 |
|---|---|---|---|---|
| 1,000 | 0.1686 | 0.0830 | 0.0469 | 0.1258 |
| **3,000** | 0.1679 | **0.1072** | 0.0814 | **0.1375** |
| 5,000 | 0.1632 | 0.1024 | 0.0901 | 0.1328 |
| 8,000 | 0.1526 | 0.0972 | 0.0865 | 0.1249 |

（overall 換算。株をまたいだ絶対値は比較できないので、列の中だけを読むこと。）

K562 だけ見れば 1,000 でも同じだが、RPE1 で −0.024、H1 で −0.035 落ちる。
3,000〜5,000 が台地で、本番はすでに 3,000。**提出枠を使う理由がない**ので
`sub_v15.vcc`（K=1,000）は取り下げ、枠は次項に回す。

## 13e. generic を少し混ぜると 3 株すべてで上がる

v12 以降、ランキング信号は target 固有の CCDT デルタ 100%、generic 応答 0% で
組んでいた（`07` §9b で「固有 > generic」だったため）。パネルで両方を**混ぜた**。

混ぜる前に単位を揃える必要がある。generic 応答は DE 表から取れば log2FC、
ライブラリのデルタとして取れば normlog で、**同じ応答が 5.9 倍違う**。
そこで `_score` は各成分を自分の平均 |·| で割ってから重みを掛けるようにした
（`BudgetConfig.generic_weight` は比であって単位ではない）。
成分ごとに 1 標的ずつではなく**全体で 1 つのスケール**で割るのが要点で、
そうしないと「転移モデルが何も言えない標的では generic が下支えする」という
混合の狙いそのものが消える。

固有 = 1.0 固定、近傍 = 20×、K = 3,000 で generic の重みを振った結果（overall 換算）:

| generic 重み | K562 | RPE1 | H1 | K562+RPE1 |
|---|---|---|---|---|
| generic のみ | 0.0841 | 0.0953 | 0.0142 | 0.0897 |
| 0.0（= v14） | 0.1679 | 0.1072 | 0.0814 | 0.1375 |
| 0.25 | 0.1768 | 0.1198 | 0.0888 | 0.1483 |
| 0.5 | 0.1828 | 0.1312 | 0.0987 | 0.1570 |
| 1.0 | **0.1850** | 0.1440 | 0.1108 | 0.1645 |
| **2.0** | 0.1837 | 0.1520 | 0.1109 | **0.1679** |
| 4.0 | 0.1720 | **0.1555** | **0.1350** | 0.1638 |

**どの重みでも 3 株すべてで v14 を上回る**（+0.007 〜 +0.054）。
重み 2.0 でのメンバー別（generic のみを基準にした差）は
`pds`・`fid`・`reach`・`jac`・`nmae` の 5 つすべてで符号が 3 株一致する。

K562（本番に最も近い株）は 1.0 で頭打ち、4.0 で落ちる。RPE1 と H1 はまだ上がる。
**2.0** を採る: 2 株平均で最良、H1 でも実質同点、K562 でも頂点との差は 0.0013。

これは `07` §9b の否定ではない。「固有 > generic」は正しかったが、
**「generic を捨てろ」という意味ではなかった**。2 つは別の情報で、
固有信号の薄い標的では generic がランキングを支える。
v12 で generic を 0 にしたのは、比較を「どちらが強いか」で切ったからで、
「両方使う」を試していなかった。

`sub_v16.vcc` = v14 + generic 2.0。取り下げた `sub_v15`（K=1,000）の枠で出す。

## 13f. パネルが答えられなかったこと

* **ランプ**（`top_margin` > `margin`、p 値の同順位を崩して `reach` を稼ぐ）。
  排出しないと p 値の順序が出ないので、高速パネルでは `key` の順序が変わらず
  差が出ない。400 細胞/摂動の H1 リファレンスで排出込みで測るしかない
  （18,080 遺伝子 × 42,400 細胞の DE は他のジョブと同時に回すとメモリが足りず
  落ちる。単独で回すこと）。→ §13g で測った。**効かない。**
* **本番の絶対値**。パネルの overall は本番より 5〜10 倍楽観的に出る
  （K562 で 0.18 に対し、本番の v12 は 0.017）。順序だけを読むこと。

## 13g. ランプは効かない（排出込みの実測）

`top_margin` を `margin` より大きく取り、呼び出し集合の先頭ほど大きい fold change を
与えれば、排出後の p 値に順序がついて `reach` が伸びる — という読みを、
H1 リファレンス（60 摂動 × 400 細胞、`n_real` 中央値 169）で排出して採点した。
両腕とも v14 相当（K=3,000・margin 1.15・近傍 20×・固有信号 K562+RPE1 から）:

| | 実現 n_pred（中央） | `fid` | `reach` | `jac` | `nmae` | mean6 |
|---|---|---|---|---|---|---|
| **平坦（top 1.15）** | 2,522 | 0.5085 | **0.1365** | 0.0320 | **0.9879** | **+0.0146** |
| ランプ（top 2.0） | 2,989 | 0.5074 | 0.1361 | 0.0363 | 1.0141 | +0.0086 |

**`reach` は 1 ミリも動かない**（0.1365 → 0.1361）。狙いは外れている。
代わりに起きたのは別のことで、先頭の倍率を上げた分だけ**呼び出しが実現しやすくなり**
（n_pred 2,522 → 2,989）、`jac` がわずかに上がって `nmae` が 0.026 悪化した。
差し引き **−0.006**。`top_margin = margin = 1.15` を維持する。

`reach` が動かない理由はおそらく、`p_adj` の同順位が「呼び出し集合の中の順序」ではなく
**呼び出し集合と非呼び出し集合の境界**で起きているため。ランプは前者しか触らない。

なお、この排出込みの H1 実測（+0.0146）は本番の v12（+0.01745）と近い。
§13b で書いたとおり**高速パネルの絶対値は 5〜10 倍楽観的**だが、
排出まで回した H1 は本番と同じ桁に落ちる。絶対値が欲しいときはこちらを使う
（1 腕あたり 15 分、メモリ 12 GB）。

## 14. パネルの答え合わせ（v14 — 近傍項）

`entry AahT3dVFyjvjq1yz4xAC`。**Overall +0.019169（Rank 127）**。v12 の +0.01745 から **+0.0017**。

| 指標 | v12 | **v14** | 差 | パネルの予測 |
|---|---|---|---|---|
| `pds_cosine` | +0.145 | **+0.1525** | +0.0075 | ↑（3 株一致） |
| `nmae` | +0.012 | +0.0176 | +0.0056 | ↑（3 株一致） |
| `fid` | −0.045 | −0.0445 | +0.0005 | 動かない |
| `reach` | +0.008 | +0.0046 | **−0.0034** | ↑（3 株一致）**外れ** |
| `jac` | −0.015 | −0.0152 | −0.0002 | 動かない |
| **Overall** | +0.01745 | **+0.019169** | **+0.0017** | +0.008〜+0.015 |

**符号は当たり、大きさは当たっていない。** パネルは overall で +0.008〜+0.015 と
言ったが実測は +0.0017。ただし §13b で書いた「高速パネルは 5〜10 倍楽観的」で
割ると +0.001〜+0.003 になり、**実測はその中に入る**。倍率の見積もりのほうは
生きているので、今後は「パネルの差 ÷ 6〜8」を本番の期待値として読む。

**5 メンバー中 4 つは向きが合った。外れたのは `reach`。** パネルでは 3 株すべてで
+0.005〜+0.007 上がると出ていたのに、本番では −0.0034 下がった。
§13g でランプが `reach` を動かさなかったのと合わせると、`reach` は
**手元のどの装置でも予測できていない**唯一のメンバー。理由はおそらく
`reach` だけが「呼び出し集合の順序」ではなく「排出後の p 値の順序」を読むためで、
高速パネルは `key` を、H1 排出は 60 摂動しか見ていない。

とはいえ `pds` と `nmae` の伸びが `reach` の目減りを 4 倍上回ったので、
差し引きは予測どおりプラス。近傍項は残す。

**（§16b で撤回）** ここで「`pds` の +0.0075 は近傍項が効いた証拠」と読んだが、
正しい `pds` 装置で測り直すと近傍項は `pds` を**まったく動かさない**。
+0.0017 という差はそもそも 1 回の提出で分解できる大きさではなく、
どのメンバーに帰属するかは決められない。

## 15. パネルの答え合わせ（v16 — generic 混合）と、`pds` の測り方の誤り

`entry QbjRDi2yexzlzGLEeKP1`。**Overall +0.001984（Rank 147）**。v14 の +0.019169 から **−0.0172**。
パネルは 3 株すべてで上がると言っていた。**大きく外した。**

| 指標 | v14 | **v16** | 差 | パネルの予測 |
|---|---|---|---|---|
| `pds_cosine` | +0.1525 | **+0.0529** | **−0.0996** | ↑ **外れ（大）** |
| `nmae` | +0.0176 | +0.0039 | −0.0137 | ↑ 外れ |
| `fid` | −0.0445 | **−0.0222** | **+0.0223** | ↑ **当たり** |
| `reach` | +0.0046 | −0.0041 | −0.0087 | ↑ 外れ |
| `jac` | −0.0152 | −0.0186 | −0.0034 | ↓ 当たり |
| **Overall** | +0.019169 | **+0.001984** | **−0.0172** | +0.016〜+0.045 |

**`fid` の予測は当たっていて、しかも大きい（+0.0223）。** 混合そのものは正しかった。
壊したのは `pds` で、−0.0996 は `fid` の利得を 4 倍上回る。

### なぜ `pds` が壊れたか

`pds` は「予測プロファイルを 300 個の真値の中から当てる検索課題」。
generic を specific の 2 倍で足すと、**呼び出し集合が全標的でほぼ同じになる**。
予算 3,000 のうち generic が上位を占め、target 固有の遺伝子が予算からこぼれる。
すると 300 個の予測が互いに似てしまい、検索が効かなくなる。

### なぜパネルはそれを見なかったか — 装置が 2 重に間違っていた

**(1) 代理指標だった。** `panel_score.py` の `pds` は「パネル平均を引いた log2FC の
コサイン順位」という自作の代理で、本物は `bulk_lognorm` 空間・`exclusion_scope="panel"`
（各行の標的だけでなく**パネル全標的の遺伝子を特徴空間から落とす**）・同順位は midrank。
代理はまさに「共有成分が増えて全予測が似る」ことに盲目だった。

`scripts/local_score.py --pds` を足して、**採点器自身の `discrimination_score` を呼ぶ**
ようにした。

**(2) それでもまだ間違えた — 摂動数が違った。** 本物の実装でも、H1 リファレンス
（60 摂動）で測ると**符号が逆に出る**:

| リファレンス | 摂動数 | generic 0.0 | generic 2.0 | 差（scaled） |
|---|---|---|---|---|
| H1 | 60 | 0.5264（+0.056） | 0.5475（+0.101） | **+0.045 逆** |
| K562 | 300 | 0.5590（+0.125） | 0.5282（+0.060） | **−0.066 ✓** |
| RPE1 | 300 | 0.5460（+0.098） | 0.5242（+0.051） | **−0.047 ✓** |
| **本番** | **300** | **+0.1525** | **+0.0529** | **−0.0996** |

**`pds` は検索課題なので、候補数がそのまま難易度。** 60 個から当てるのと
300 個から当てるのは別の問題で、60 では「共有成分が増えても当てられる」。
`n` が合っていない装置は符号すら保証しない。

`pds` は pseudobulk しか使わないので、**100 細胞/摂動でも十分**（§13b で
「排出依存の量は測れない」と書いたが、`pds` はその例外）。
1 腕あたり排出 4 分 + 採点 1 分。

### これからの測り方

| 測るもの | 装置 | 根拠 |
|---|---|---|
| `fid`・`jac`・`nmae` | `panel_score.py`（3 株、高速） | `cell_eval2` の関数と照合済み |
| `pds` | `local_predict.py` → `local_score.py --pds`（K562/RPE1、300 摂動） | 採点器本体、`n` 一致、本番の符号と幅を再現 |
| `reach` | **なし** | §14 参照。どの装置も外す |
| 絶対値 | H1 排出（400 細胞/摂動） | §13g、本番と同じ桁 |

v16 は取り下げ、**v14（+0.019169）が現行最良**。
混合の `fid` 利得 +0.0223 は本物なので、`pds` を壊さずに取りに行く方法を探す:
generic の重みを下げる（0.25〜0.5）、あるいは予算を増やして固有遺伝子を
予算から追い出さないようにする（K=6,000）。どちらも上の 2 装置で提出前に測れる。

## 16. `pds` を押す — 呼び出しの「大きさ」に標的を入れる

`pds` は v14 で +0.1525、6 メンバーの合計 0.115 のうち単独でそれを超えている
（`fid` −0.0445 と `jac` −0.0152 が引き下げている）。生値は 0.5717 で、複製の
0.95〜0.97 に対してまだ大きく空いている。**残っている最大の余地はここ。**

呼び出しに与える大きさは `significance_magnitude`（発現量だけの関数）× ramp なので、
**どの標的も同じ大きさを出していて、違うのは符号と選ばれた遺伝子だけ**だった。
`pds` は 300 個の真値の中から自分を当てる検索課題なので、全標的で共通の座標は
情報を持たない。そこで `call_size_gamma` を足した: 呼び出しの大きさをその標的自身の
ランキングスコアで割り増す（下限 1.0 = しきい値を割らない、上限 `max_call_scale`）。
**呼び出し集合も符号も変わらない**ので `fid` と `jac` は定義上動かず、
危ないのは `nmae` だけ… のはずだった。

### 測定（K=3,000、generic 0、近傍 20×）

`pds` は K562（300 摂動）、DE メンバーは H1 排出（400 細胞/摂動）。§15 の使い分け。

| 配合 | `pds`（K562, scaled） | H1 `fid` | H1 `reach` | H1 `jac` | H1 `nmae` | H1 DE 計/6 |
|---|---|---|---|---|---|---|
| なし（= v14） | +0.1253 | 0.5085 | 0.1365 | 0.0320 | 0.9879 | **0.0146** |
| **γ0.5 上限 3** | **+0.1944** | 0.4925 | 0.1037 | 0.0766 | 1.0078 | **0.0141** |
| γ1.0 上限 2 | +0.1784 | 0.4930 | 0.1096 | 0.0776 | 1.0168 | 0.0135 |
| γ1.0 上限 3 | +0.2012 | 0.4935 | 0.1074 | 0.0779 | 1.0476 | 0.0049 |
| γ1.0 上限 6 | +0.2195 | 0.4942 | 0.1068 | 0.0778 | 1.0993 | −0.0092 |

RPE1 でも確認: γ なし 0.5460（+0.0977）→ γ1.0 上限 3 で 0.5852（+0.1811）、
**+0.0834**（K562 の +0.0759 より大きい）。2 株とも同符号・同程度。

**`pds` は上限とともに単調に増え、`nmae` は上限とともに単調に悪化する。**
交点は上限 2〜3。γ0.5・上限 3 が最良で、`pds` +0.069 に対し DE 側の代償は
合計 −0.003 しかない。

「集合も符号も変わらないから `fid`・`jac` は動かない」は**外れた**。
大きさを上げると**実際に有意になる呼び出しが増える**（H1 で n_pred 2,522 → 2,784）ので、
`fid` は分母が増えて下がり、`jac` は上がる。ランプ（§13g）で見たのと同じ機構。

### 見積もり

| メンバー | γ0.5 上限 3 の効き |
|---|---|
| `pds` | +0.069（K562 実測）〜 +0.105（§15 の ×1.5 換算） |
| `fid` | −0.053 |
| `nmae` | −0.033 |
| `jac` | +0.010（本番の `n_real` 275 で見積もり）〜 +0.124（H1 実測、信用しない） |
| `reach` | −0.037（H1。§14 のとおり信用しない） |

中央値の読みで **Overall +0.011**、悲観側（`jac` の利得を数えず `reach` を満額引く）で
−0.003。**上振れが大きく下振れが小さい**ので 1 枠使う価値がある。

`sub_v17.vcc` = v14 + γ0.5・上限 3。

### 同時に否定されたもの

**予算 K を増やす**のは γ なしなら `pds` に効く（K562: 3,000 で +0.1253、
**6,000 で +0.1441**、9,000 以上は 8,562 遺伝子しか検定対象がないので頭打ち）。
ただし γ を入れると効かなくなる（γ1.0 上限 3 で 3,000 が +0.2012、6,000 は +0.1985）。
DE 側の代償（`fid`・`jac`）を考えると単独では割に合わないので、K=3,000 のまま。

**generic の混合**は `pds` を単調に壊す。K562 で 0.559（重み 0）→ 0.559（0.25）→
0.554（0.5）→ 0.540（1.0）→ 0.528（2.0）。重み 0.25 なら `pds` はほぼ無傷で
`fid` は +0.009 相当取れるが、合計 +0.001 程度で枠を使う価値はない。

## 16b. 近傍項は `pds` を動かしていなかった（自分の主張の撤回）

近傍項の重み 20× は**間違った代理指標**（§15 で捨てたコサイン順位）で選んだ値だった。
採点器本体の `discrimination_score` を K562（300 摂動）で回して測り直すと:

| 近傍重み | `pds`（γ0.5・上限 3・K=3,000） |
|---|---|
| 0 | 0.5913 |
| 20（= v14/v17） | 0.5914 |
| 50 | 0.5914 |
| 100 | 0.5913 |
| 200 | 0.5913 |

**完全に平坦。** どの重みでも 4 桁目までしか動かない。

装置が見られないだけではない。K562 リファレンスの近傍密度は本番とほぼ同じ
（100 kb 以内の中央値 2 個、1 個以上ある標的 75%。本番は中央値 2 個・76%、docs/07 §12d）。
**見えるはずのものが見えていない**のではなく、効果が小さすぎる。

理由は単純で、`pds` は 8,500 次元・非ゼロ 3,000 のコサインを 300 個の候補で
順位付けする。標的あたり 2 座標を変えても順位はまず動かない。
§12 で測った「10–25 kb で 2.8 倍の濃縮」という生物学は本物だが、
**この指標の分解能の下にある。**

したがって §14 の「近傍項が `pds` を +0.0075 押した」は**撤回する**。
v12 → v14 の Overall +0.0017 がどのメンバーから来たかは、
1 回の提出では分解できない。近傍項は害がない（どのメンバーも動かさない）ので
残すが、**効いているとは言えない**。

教訓: 代理指標で選んだハイパーパラメータは、装置を直したら**全部測り直す**。

## 16c. 代理指標で選んだつまみを全部測り直した

`pds` 装置が直った（§15）ので、これまで代理指標で決めた値を K562（300 摂動、
γ0.5・上限 3・K=3,000）で測り直した。

**近傍項の重み** — 完全に平坦（§16b）。0 でも 200 でも `pds` は 0.5913〜0.5914。

**propensity の指数** — ランキングは `propensity × |signal|` で、propensity は
標的に依存しない。だから重く効かせるほど全標的が同じ集合を宣言する。
指数を 1 未満にすれば「検出されやすさ」を「標的らしさ」と交換できる、
という読みだったが**外れ**:

| 指数 | 1.0 | 0.75 | 0.5 | 0.25 | 0.0 |
|---|---|---|---|---|---|
| `pds` | **0.5914** | 0.5900 | 0.5879 | 0.5885 | 0.5883 |

下げるほど悪い。検出されにくい遺伝子はリファレンス側でも当てにならないので、
「標的らしさ」を買っても情報にならない。1.0 のまま。

**パネル平均の引き算（`shared_shrink`）** — v11 → v12 を倍増させたつまみ。
これも代理指標で 1.0 に決めていた:

| 引き算 | 0.0 | 0.5 | **0.75** | 1.0 | 1.25 | 1.5 |
|---|---|---|---|---|---|---|
| `pds` | 0.5600 | 0.5839 | **0.5930** | 0.5914 | 0.5907 | 0.5875 |

0.75〜1.25 が台地で、頂点は 0.75（現行 1.0 との差は scaled +0.003）。
**枠を使う差ではない**ので 1.0 のまま。

### 装置の 2 度目の検証、と換算率について

引き算 0.0 → 1.0 は本番で `pds` +0.077（v11 → v12、docs/07 §11）。
K562 装置は +0.067。**符号も幅も合う。** §15 の混合の件（K562 −0.066 に対し本番 −0.100）
と合わせて 2 例で検証できた。

ただし換算率は 0.87 と 1.52 でばらつく。**「K562 の値 × 1.5」を係数として使うのは誤り**で、
装置が保証するのは**符号と桁**まで。§16 の見積もりもその幅で読むこと。

## 16d. 装置の 3 つ目の欠陥 — 別のモデルを測っていた

`local_predict.specific_signal` が転移モデルの設定をハードコードしていた
（`n_components=30, n_neighbours=100, neighbour_power=2.0`）のに対し、
提出物は `ModelConfig()` の既定（`30, 25, 3.0`）で作られていた。
**手元の測定はずっと別のモデルを測っていた。**

`ModelConfig()` 既定に合わせ、`--alpha` / `--n-components` / `--n-neighbours` /
`--neighbour-power` を CLI に出した。v17 の変更を正しいモデルで測り直すと:

| | 誤ったモデル（100 近傍・power 2） | **正しいモデル（25 近傍・power 3）** |
|---|---|---|
| γ なし | 0.5590（+0.1253） | 0.5583（+0.1239） |
| γ0.5・上限 3 | 0.5914（+0.1944） | **0.5963（+0.2048）** |
| 差（scaled） | +0.069 | **+0.081** |

**結論は変わらず、幅はむしろ広がった。** v17 はそのまま出す。

これで装置の欠陥は 3 つ見つけて 3 つとも直したことになる:
代理指標（§15）、摂動数（§15）、モデル設定（ここ）。
どれも「測れているつもりで別のものを測っていた」類で、
**リーダーボードに出して初めて気づいた**のは 1 つ目だけ。
残り 2 つは、1 つ目を直した拍子に芋づるで見つかった。

## 17. 転移モデルのチューニングは転移しない

`pds` 装置が使えるようになったので、`docs/07` §9d で「方向一致率では平坦」と
結論した転移モデルのハイパーパラメータを、**感度の高い読み出し**で振り直した。
K562（300 摂動、γ0.5・上限 3・K=3,000・shrink 1.0）:

| 変更 | `pds` | scaled | 基準との差 |
|---|---|---|---|
| 基準（STRING 400・30 成分・25 近傍・power 3） | 0.5963 | +0.2048 | — |
| STRING 150 | 0.5931 | +0.1979 | −0.007 |
| STRING 700 | 0.6002 | +0.2131 | +0.008 |
| STRING 900 | 0.6006 | +0.2139 | +0.009 |
| 近傍 10 / 50 / 100 | 0.5962 / 0.5944 / 0.5918 | | −0.000 / −0.004 / −0.010 |
| power 1 | 0.5986 | +0.2096 | +0.005 |
| power 5 | 0.5950 | +0.2020 | −0.003 |
| **成分 100 / 200 / 400** | 0.6057 / **0.6069** / 0.6060 | | **+0.020 / +0.023 / +0.021** |
| **全部（900・200 成分・power 1）** | **0.6147** | **+0.2441** | **+0.039** |

K562 だけ見れば大きい。低ランク成分を 30 → 200 に増やすのが主因で、
組み合わせると +0.039 — v17 の γ 効果（+0.081）の半分に届く。

**RPE1 で確かめたら符号が逆だった。**

| 変更 | K562 | RPE1 |
|---|---|---|
| 成分 200 | **+0.023** | −0.003 |
| STRING 900 | +0.009 | **−0.017** |
| power 1 | +0.005 | +0.001 |
| 全部 | **+0.039** | **−0.019** |

**符号が一致するのは `power 1` だけで、しかも +0.005 / +0.001 と誤差の中。**
K562 の +0.039 は**その株に固有**で、転移しない。

これは `docs/07` §9d の否定ではなく、その裏取りになっている。
方向一致率で平坦だったものは、`pds` で振っても本質的には平坦で、
片方の株で出た山は**過学習の山**だった。2 株持っていなければ
+0.039 を本物だと思って 1 枠使っていた。

**したがって v18 に転移モデルの変更は入れない。** 枠 2 は v17 の結果を見て
γ / 上限の軸で決める（トリガ本文の分岐規則のまま）。

残る壁は変わらない。オラクル類似度なら discrimination 0.748 まで行く
（`src/vcc2026/coexpression.py`）ので、**モデルの形ではなく類似度が壁**であり、
STRING のしきい値・近傍数・成分数はどれもその類似度を良くしていない。
必要なのは別種の類似度であって、同じ類似度のつまみではない。

## 18. 9 回目の提出（v17）— 自己ベスト更新、そして装置の答え合わせ

`entry 3KMlFczweycn1WG0vNuj`。**Overall +0.023713（Rank 171）**。v14 の +0.019169 から **+0.0045**。

| 指標 | v14 | **v17** | 差 | 事前の予測 | 当たったか |
|---|---|---|---|---|---|
| `pds_cosine` | +0.1525 | **+0.1679** | +0.0154 | +0.081 | 符号 ○、幅は 1/5 |
| `nmae` | +0.0176 | −0.0144 | **−0.0320** | −0.033 | **ほぼ完全に的中** |
| `fid` | −0.0445 | **−0.0312** | **+0.0133** | −0.053 | **符号が逆** |
| `reach` | +0.0046 | **+0.0220** | +0.0174 | −0.037 | 符号が逆（測れないと宣言済み） |
| `jac` | −0.0152 | **−0.0020** | +0.0132 | +0.010 | **符号も幅も的中** |
| **Overall** | +0.019169 | **+0.023713** | **+0.0045** | +0.011 | 符号 ○ |

**内訳が予測と違う形で当たった。** `pds` は上がったが幅は 1/5。代わりに
「動かないはず」と思っていた `fid`・`jac`・`reach` が**そろって改善**し、
その合計（+0.044）が `nmae` の代償（−0.032）を上回って黒字になった。

理由は §16 で書いた機構そのもの: 大きさを上げると**実際に有意になる呼び出しが増える**。
`n_pred` が増えれば `jac` の分子が増え、`reach` は深く歩けるようになる。
`fid` も上がったのは、増えた分が上位ランクの呼び出し（当たりやすい）だったから。
つまり **v14 の呼び出しは有意化しきれていなかった**。

### 装置ごとの当たり外れ（これが次に効く）

| 指標 | 装置 | 予測 | 実測 | 換算率 |
|---|---|---|---|---|
| `nmae` | H1 排出（400 細胞/摂動） | −0.033 | −0.032 | **≈1.0** |
| `jac` | H1 排出（本番の `n_real` で補正した見積もり） | +0.010 | +0.013 | ≈1.3 |
| `pds` | K562（300 摂動） | +0.081 | +0.015 | **≈0.19** |
| `fid` | H1 排出 | −0.053 | +0.013 | **符号が逆** |
| `reach` | — | −0.037 | +0.022 | 符号が逆 |

**`nmae` は H1 排出でそのまま読める。** これは大きい。
**`pds` は K562 が符号を当てるが幅は 5 倍過大。**
**`fid` は H1 では読めない** — H1 の平均 `n_real` は 1,028 で、
`max(n_pred, n_real)` の分母が本番（275）と違う領域に入るため。
§15 で「`fid` は H1 排出で測る」と書いたのは訂正する。**`fid` を読める装置はまだない。**

### 枠 2 は分岐規則どおりにはしない

トリガに書いた分岐は「`pds` も Overall も上がったら γ1.0・上限 3 へもう一段」だったが、
上の換算率を入れると**それは負ける**:

- `pds`: (0.0759 − 0.0691) × 0.19 = **+0.0013**
- `nmae`: H1 で 1.0078 → 1.0476、scaled **−0.066**（換算率 1.0 なのでそのまま効く）
- 差し引き −0.065 → Overall **−0.011**

γ1.0・上限 2 も同様に負ける（`pds` −0.003、`nmae` −0.015）。
**γ0.5・上限 3 はこの軸の頂点**で、どちらに動かしても悪くなる。
規則より実測を優先する。

## 19. 10 回目の提出（v18）— ノイズ床を測る

`entry Yp1LEmLGYLQzKK6rWhXJ`。**v17 と完全に同じ設定で、排出のシードだけ 0 → 1 に変えたもの。**
モデルも呼び出し集合の決め方も同一で、違うのは「どのコントロール細胞を引いて、
どう乱数を振ったか」だけ。

**Overall +0.025179。v17 の +0.023713 から +0.00147。**

| 指標 | v17（seed 0） | v18（seed 1） | 差 = ノイズ |
|---|---|---|---|
| `pds_cosine` | +0.16788 | +0.16550 | −0.0024 |
| `nmae` | −0.01435 | +0.00143 | **+0.0158** |
| `fid` | −0.03117 | −0.03190 | −0.0007 |
| `reach` | +0.02196 | +0.01828 | −0.0037 |
| `jac` | −0.00204 | −0.00224 | −0.0002 |
| **Overall** | +0.023713 | **+0.025179** | **+0.00147** |

**Overall のノイズ床は ±0.0015。`nmae` だけが ±0.016 と桁違いに暴れる。**
`fid` と `jac` はほぼ完全に安定（±0.0007、±0.0002）、`pds` が ±0.0024、`reach` が ±0.0037。

### これまでの結論の読み直し

| 比較 | 差 | ノイズ床（±0.0015）との比 | 判定 |
|---|---|---|---|
| v12 → v14（近傍項） | +0.0017 | **1.1 倍** | **ノイズと区別できない** |
| v14 → v17（大きさの傾斜） | +0.0045 | 3.0 倍 | 本物 |
| v11 → v12（パネル平均の差し引き） | +0.0089 | 5.9 倍 | 本物 |
| v14 → v16（generic 混合） | −0.0172 | 11 倍 | 本物（悪化） |

§16b で近傍項の `pds` 寄与を撤回したが、**Overall の +0.0017 そのものがノイズの範囲**だった。
1 枠 1 実験では ±0.0015 未満の変更は検出できない。今後は
**「手元で ±0.008 相当の差が見えないものは提出しない」**を基準にする
（`pds` なら K562 で 0.04 以上、換算率 0.19 を考慮して）。

そして §18 で「`nmae` は H1 排出で換算率 1.0」と書いたのは**運**だった。
予測 −0.033 に対し実測 −0.032 だったが、その実測自体が ±0.016 揺れる。
`nmae` について言えるのは「H1 は符号と桁を当てる」までで、
±0.016 より細かい話はできない。

### 枠を 1 つ使う価値はあったか

締切まで 10 週、1 日 2 枠で約 140 枠ある。そのうち 1 枠で、
**過去 9 回すべての解釈と、今後すべての判断基準**が定まった。
しかも副作用として v18 が自己ベスト（+0.025179）になったので、
スコア上も損をしていない。

## 20. 共必須性 — 2 株で符号が揃った初めての表現、ただし小さい

`docs/07` §9d と `docs/08` §17 で、STRING のつまみ（しきい値・近傍数・重み・成分数）は
どれも 2 株で符号が揃わないと確定した。オラクル類似度なら discrimination 0.748 まで
行くので、壁は**類似度そのもの**。共発現は信号ゼロと測定済み（`coexpression.py`）。
残るのは「別種の類似度」で、DepMap を使った。

**共必須性（co-essentiality）**: 2 つの遺伝子は、ノックアウトが 1,178 の細胞株で
同じように増殖を変えるなら機能的に近い。STRING が「誰かが論文に書いた」と言うのに対し、
これは**同種の実験による測定**で、しかも複合体のメンバーはアノテーションが無くても
ほぼ同一のフィットネスプロファイルを持つ — STRING の近傍プロファイルが最も苦手な場合。

DepMap 24Q4 の `CRISPRGeneEffect.csv`（429 MB、1,178 株 × 17,289 遺伝子）から
`src/vcc2026/coessentiality.py` で特徴を作る。各遺伝子のプロファイルを中心化して
L2 正規化するので、モデルが取るコサインはそのまま相関になる。

### 測定（K562/RPE1 の 300 摂動、γ0.5・上限 3・K=3,000）

| 類似度 | K562 | RPE1 | 両方 + |
|---|---|---|---|
| STRING 400（現行） | +0.2048 | +0.1572 | — |
| 共必須性のみ | +0.1839（**−0.021**） | +0.1928（**+0.036**） | ✗ |
| 近傍プロファイル版のみ（top 50） | +0.1979（−0.007） | +0.1802（+0.023） | ✗ |
| STRING + 共必須性 0.5 | +0.2141（+0.009） | +0.1610（+0.004） | ✓ |
| **STRING + 共必須性 1.0** | **+0.2167（+0.012）** | +0.1698（+0.013） | ✓ |
| **STRING + 共必須性 2.0** | +0.2098（+0.005） | **+0.1794（+0.022）** | ✓ |
| STRING + 共必須性 4.0 | +0.1962（−0.009） | +0.1890（+0.032） | ✗ |
| STRING + 近傍版 1.0 | +0.2173（+0.013） | +0.1610（+0.004） | ✓ |

**単独では STRING に負け、混ぜると両方で勝つ。** 2 つは別の情報で、
共必須性だけにすると K562 が落ち、STRING だけにすると RPE1 が落ちる。
重み 1〜2 が両株プラスの範囲で、4 以上は K562 が負ける。
近傍プロファイル化は生プロファイルより**良くならない**（DepMap の相関は
STRING のスコアと違って既に連続量なので、間引く意味がない）。

**§17 以来、2 株で符号が揃った初めての表現の変更。** 判定基準（両株で同符号）を
満たしたのはこれだけ。

### ただし小さい

平均 +0.013〜0.014 scaled。§18 の換算率 0.19 を掛けると本番の `pds` メンバーで
+0.0025〜0.0027、Overall で **+0.0004〜0.0005**。
**§19 のノイズ床 ±0.0015 の 1/3**。1 回の提出では検出できない。

つまり「本物だが測れない」。壁の性質は変わっていない: 類似度を STRING から
共必須性に替えても、`pds` の生値 0.578 から複製上限 0.95 までの距離は
ほとんど埋まらない。**必要なのは別の類似度ではなく、桁が違う類似度**らしい。

## 21. 摂動ごとの予算 — 天井は高いが、届かない

`docs/06` に「2026 パネルの 300 遺伝子は Replogle と 1 つも重ならないので
応答の強さは転移元から引けず、予算は固定にせざるを得ない」と書いた。
**DepMap はその制約を破る。**

平均 gene effect（1,178 株での増殖への影響）は `n_real` を予測する:
Spearman **+0.383（K562）・+0.290（RPE1）・+0.473（H1）**、3 株とも同符号。
DepMap は本番 300 標的の約 96% をカバーする。
しかも回帰の**傾きは株をまたいでほぼ同じ**（K562 −1.446、RPE1 −1.497）で、
違うのは切片＝その文脈の全体的な強さだけ。相対的な強さは転移する。

さらに良い予測子がモデル自身の中にあった。**ランキングスコアの 3,000 位の値**が
Spearman **+0.432 / +0.416** で DepMap を上回る。両方を z 化して足すと **+0.513 / +0.462**。

### 天井

標的ごとの予算を `n_real` そのもの（オラクル）にすると `jac` は:

| | 現行（一律 3,000） | K = n_real | K = 2×n_real |
|---|---|---|---|
| K562 | 0.0358 | 0.0518 | **0.0541** |
| RPE1 | 0.0601 | 0.0810 | **0.0829** |

raw で +0.018 / +0.023、scaled で **+0.049 / +0.062**。換算率 1.3 を掛けると
Overall **+0.011 相当**。v12 以降で最大の梃子になるはずだった。

### 届かない

Spearman 0.51 の予測子でも、一律予算にほとんど勝てない:

| | 最良の標的別予算 | 同じ平均の一律予算 |
|---|---|---|
| K562 | 0.0397 | 0.0390 |
| RPE1 | 0.0623 | 0.0560 |

**+0.0007 / +0.0063 raw。** 天井の 1/10 以下。しかも予算のばらつきを強くする
（spread 1.0 → 2.0）と**急激に悪化する**（K562 0.0326、RPE1 0.0296）。

理由は `jac(K)` が各標的の最適点の近くで平坦なこと。桁で外した標的の損失が、
当てた標的の利得を上回る。`n_real` は log で sd 2.4 に広がっていて、
Spearman 0.5 の予測子の残差は log で sd 2.1 — **ほとんど狭まっていない**。

`fid` はそもそも標的別予算を求めない。`k / max(n_pred, n_real)` は
`n_pred ≥ n_real` である限り方向一致率そのもので、予算に依存しない。

**この線は閉じる。** DepMap による `n_real` の予測は本物（3 株で同符号、p < 1e-7）だが、
`jac` を動かすには 1 桁足りない。オラクルなら +0.011 あるので、
**`n_real` を log で 1 桁以内に当てられる予測子が現れれば、ここは最大の梃子に戻る。**

## 22. 11・12 回目の提出（v19・v20）— 共必須性は乗った。パネルの 12 倍

§20 で「本物だが測れない」と書いた共必須性ブレンドを、**シードを揃えた対で 2 回**出した。
1 回ではノイズ床 ±0.0015 に埋もれるので、同じシードのブレンド無しと突き合わせ、
2 つの差を平均してノイズを ±0.001 に落とす設計。

| 提出 | シード | ブレンド | Overall | 対応する対照 | 差 |
|---|---|---|---|---|---|
| v19 | 0 | 1.0 | **+0.031098** | v17（+0.023713） | **+0.0074** |
| v20 | 1 | 1.0 | +0.030195 | v18（+0.025179） | **+0.0050** |

**平均 +0.0062。ノイズ床 ±0.001 の 6 倍。** 効果は確定した。v19 が自己ベスト（順位 178）。

メンバー内訳（対応するシードどうしの差）:

| 指標 | v19−v17 | v20−v18 | 平均 | ノイズ（対で ±） |
|---|---|---|---|---|
| `pds` | +0.0267 | +0.0127 | **+0.0197** | ±0.0017 |
| `nmae` | +0.0166 | +0.0079 | +0.0122 | ±0.0112 |
| `fid` | +0.0046 | +0.0051 | +0.0048 | ±0.0005 |
| `reach` | −0.0042 | +0.0027 | −0.0007 | ±0.0026 |
| `jac` | +0.0006 | +0.0010 | +0.0008 | ±0.0001 |

利得はほぼ `pds` 1 本。**11 倍のノイズ床。** `fid` の +0.0048 も小さいが 9 倍で本物。

### パネルは符号を当て、桁を 12 倍外した

§20 のパネルは K562 +0.012 / RPE1 +0.013 scaled を示し、換算率 0.19 から
Overall **+0.0005** と予測した。実測は **+0.0062**。
**符号は合っていたが桁が違う。** 換算率 0.19 はこの変更には当てはまらなかった。

## 23. 装置の 4 つ目の欠陥 — パネルは類似度を採点していない

なぜ 12 倍外したのか。`src/vcc2026/model.py` の予測は 2 本の腕に分かれる:

```
b = w_obs / (w_obs + confidence_prior)      # w_obs = その標的自身の実測の重み
d = b * d_direct + (1 - b) * d_knn          # w_obs = 0 なら d = d_knn
```

* **D 腕** `d_direct` — **その標的自身**の実測シグネチャを別の株からリベースしたもの
* **K 腕** `d_knn` — 類似度で選んだ近傍から組み立てたもの

**共必須性ブレンドは類似度、つまり K 腕にしか入らない。** そして:

| | 標的が転移元でも実測されている割合 | 実際に効く腕 |
|---|---|---|
| K562 パネル | **300 / 300 = 100%** | ほぼ D |
| RPE1 パネル | 249 / 300 = **83%** | ほぼ D |
| **本番 2026** | **0 / 300 = 0%** | **100% K** |

`docs/06` に「2026 パネルの 300 遺伝子は Replogle と 1 つも重ならない」と書いてある。
その事実は予算の話（§21）としてだけ扱っていたが、**本当の意味はこちらだった**:
本番では `w_obs = 0` が 300 標的すべてで成り立ち、`b = 0`、予測は**全部 K 腕**。
一方パネルの標的は Replogle 由来なので、K562 を採点するとき RPE1 が同じ遺伝子を
測っており、`b > 0` で D 腕が答えを持っていってしまう。

**パネルは、本番には存在しない領域を採点していた。** 類似度をどう変えても
`(1 − b)` 倍に薄まって出てくる。§20 の「本物だが小さい」の「小さい」は、
共必須性の性質ではなく**装置の希釈率**だった。

これで §17 の「転移モデルのチューニングは転移しない」も読み直す必要がある。
STRING のしきい値・近傍数・近傍の重み・低ランク成分数 — **全部 K 腕のつまみ**で、
すべて希釈された状態で振っていた。2 株で符号が一致しなかったのは、
モデルの形の問題ではなく、**測っていた信号が薄すぎた**からかもしれない。

### 直し方

パネルのソース側から、**採点する標的自身のシグネチャを 1 つずつ抜く**（leave-one-out）。
`w_obs = 0` を強制すれば `b = 0` になり、パネルは本番と同じ「全部 K 腕」の領域を測る。
株ごと落とす現行の hold-out（`sources_for`）は株の漏れは防ぐが、
**同じ遺伝子が別の株で測られている漏れは防いでいない**。

これが直れば、類似度まわりの変更を提出枠を使わずに選別できる。
§17 で閉じた線（STRING のつまみ、近傍数、成分数）は**全部開き直す価値がある**。

## 24. パネルを直した — 効果は 3.7 倍で見えるようになった

`--holdout-target-signatures`（`scripts/local_predict.py`・`scripts/panel_score.py`）を足した。
ソース側の各株から、**採点する 300 標的自身のシグネチャだけ**を抜く。
K562 を採点するときは 313 本を抜いて 2,374 本が近傍として残る（RPE1 は 264 / 2,093）。
株ごと落とす現行の hold-out はそのまま。これで `w_obs = 0`、`b = 0` が 300 標的すべてで
成り立ち、本番と同じ「全部 K 腕」になる。

### 直す前と直した後（`pds` scaled、シード 0）

| | STRING のみ | ＋共必須性 1.0 | 差 |
|---|---|---|---|
| K562・**直す前** | +0.2048 | +0.2167 | +0.012 |
| K562・**直した後** | +0.1286 | +0.1839 | **+0.0553** |
| RPE1・**直す前** | +0.1572 | +0.1698 | +0.013 |
| RPE1・**直した後** | +0.0862 | +0.1242 | **+0.0380** |

**同じ変更が 3.7 倍の大きさで見える。** 平均 +0.0125 → **+0.0467**。

抜いた分だけ絶対値も下がる（K562 −0.076、RPE1 −0.071）。
それが標的自身の実測が運んでいた点数で、本番では 1 標的たりとも手に入らないもの。

### 換算率はまだ 1 点しかない

本番の実測は `pds` メンバーで +0.0197（§22）。

| 装置 | 読み | ×0.19（§18 の率） | 実測との比 |
|---|---|---|---|
| 直す前のパネル | +0.0125 | +0.0024 | **8 倍低い** |
| 直した後のパネル | +0.0467 | +0.0089 | 2.2 倍低い |

**大半は埋まったが、まだ 2 倍残っている。** 率 0.19 は直す前のパネルで
呼び出し集合の変更から求めたもので、直した装置にそのまま使えるとは限らない。
この 1 点だけで解くと 0.42。**点が 1 つでは率とは呼べない。**
次に類似度側の変更を出すときは、直したパネルの読みと本番の差をもう 1 点取ること。

### `tests/test_transfer_model.py` に固定した

`test_a_target_measured_elsewhere_ignores_the_similarity` — 類似度を実物と
シャッフル版で入れ替えて、予測がどれだけ動くかを 2 つの領域で比べる。
シミュレータでは**抜くと 4.1 倍動く**（0.093 → 0.382）。
希釈が戻ったらこのテストが落ちる。

### §17 を開き直す

「転移モデルのチューニングは転移しない」（§17）で閉じた線は、
STRING のしきい値・近傍数・近傍の重み・低ランク成分数 — **全部 K 腕のつまみ**。
希釈された装置で 2 株の符号が揃わなかったことは、もう反証として使えない。
**直した装置で振り直す価値がある。** これが次の作業。

## 25. 直した装置で §17 を振り直した — §17 は正しかった

§24 で「§17 で閉じた線は希釈された装置で振っていたので開き直す価値がある」と書いた。
3.7 倍感度の上がった装置で振り直した結果、**§17 の結論は覆らなかった。**
基準は LOO・ブレンド 1.0（K562 +0.1839 / RPE1 +0.1242）:

| 変更 | K562 | RPE1 | 2 株で符号一致 |
|---|---|---|---|
| 低ランク成分 200 | **±0.0000** | **±0.0000** | — 効果なし（下記） |
| STRING 900 | −0.0141 | −0.0241 | ✓ **どちらも負** |
| 近傍の重み power 1 | **+0.0192** | −0.0058 | ✗ 符号が反転 |
| 全部（900・200 成分・power 1） | +0.0243 | −0.0122 | ✗ 符号が反転 |

**`power 1` は §17 の +0.005 / +0.001 が +0.0192 / −0.0058 になっただけ。**
装置が 4 倍鋭くなって、K562 の山も 4 倍高く見えたが、**RPE1 では相変わらず負**。
同じ過学習の山を、より鮮明に見ているにすぎない。

**§24 の「開き直す価値がある」は取り下げる。** 振り直す価値はあった
（確かめずに閉じたままなのは誤りだった）が、答えは同じだった。

### 新しく確定したこと

**STRING 900 は 2 株とも負。** §17 では +0.009 / −0.017 で判断できなかったが、
直した装置では −0.0141 / −0.0241 と**符号が揃って負**。しきい値 400 のままでよい。

### 低ランク成分数は本番では最初から効かない

成分 200 が**ビット単位で基準と同じスコア**を返した。コードを追うと理由が出る
（`src/vcc2026/model.py`）:

```
if cfg.coverage_aware_knn:              # 既定 True
    d_knn = _covered_average(...)       # 基底を通らない
else:
    d_knn = basis.reconstruct(...)      # ここでしか基底を読まない
...
if observed[i]:
    d_direct = basis.denoise(...)       # と、ここ
```

基底を読むのは 2 か所だけで、片方は既定で無効、もう片方は `observed`
＝**標的自身が転移元で実測されている**ときにしか通らない。
本番の 300 標的は 1 つも実測されていないので `observed` は全部 False。
つまり **`n_components` は本番の予測を 1 つも動かせない。**

§17 が K562 で見た成分 200 の +0.023 は、`denoise` 経由の効果だった
——**本番が絶対に通らない経路**。§23 の希釈は、効果を薄めるだけでなく、
**存在しない効果を作り出してもいた。**

### 壁は動いていない

STRING のしきい値・近傍数・近傍の重み・低ランク成分数は、
**直した装置でも 1 つも 2 株で揃わない**（900 は揃って負）。
方向の的中率が 0.52〜0.57 で頭打ちなのは、モデルの形の問題ではない。
共必須性（§20・§22）が効いたのは**別種の類似度**を足したからで、
同じ類似度のつまみを回したからではない。次に探すべきは 3 つ目の類似度。

## 26. 3 つ目の類似度（組織発現）— 本物だが共必須性と重なる

§25 で「つまみは全部死んだ。効いたのは別種の類似度を足したことだけ」と書いた。
そこで 3 つ目を探した。**GTEx の 68 組織の中央値 TPM** から遺伝子ごとの
発現プロファイルを作り、その相関を類似度にする（`src/vcc2026/tissue.py`、
`scripts/build_tissue_features.py`）。

3 つの類似度はそれぞれ別種の証拠に基づく:

| 類似度 | 「関係がある」の根拠 |
|---|---|
| STRING | 文献・データベースが関係を記載している |
| DepMap 共必須性 | 1,178 株でノックアウトが増殖を同じように変える |
| **GTEx 組織発現** | **68 組織で発現が一緒に上下する** |

カバー率は 3 つの中で最も良い（本番パネル 300 標的の 99% / 96%。共必須性は 96% / 92%）。
`--features` / `--feature-blend` を繰り返し指定できるようにして、いくつでも重ねられる。

### 直したパネルでの選別（`--holdout-target-signatures`、シード 0）

| 構成 | K562 | RPE1 |
|---|---|---|
| STRING のみ | +0.1286 | +0.0862 |
| STRING + 共必須性 1.0（**現行**） | **+0.1839** | **+0.1242** |
| STRING + 組織 1.0 | +0.1624 | +0.0897 |
| STRING + 共必須性 + 組織 1.0 | +0.1517 | +0.0915 |
| STRING + 共必須性 + 組織 0.5 | +0.1662 | +0.1164 |

**組織発現は単独では本物**（STRING のみに足して K562 +0.0338 / RPE1 +0.0035、
2 株とも正）。**だが共必須性には勝てず、足しても必ず悪くなる**:

| 現行に組織を足す | K562 | RPE1 | 2 株一致 |
|---|---|---|---|
| 重み 1.0 | −0.0322 | −0.0327 | ✓ **どちらも負** |
| 重み 0.5 | −0.0177 | −0.0078 | ✓ **どちらも負** |

**符号が 2 株で揃って負。** この時点では「組織発現が知っていることは共必須性が
すでにもっとよく知っている」と読んだ。**その読みは §27 で間違いだと分かった。**
結論（足さない）は変わらないが、理由が違う。

### 装置が初めて「出す前に」候補を落とした

これは直したパネル（§24）で**提出枠を 1 つも使わずに却下できた最初の候補**。
希釈された装置なら K562 の +0.0338 を見て出していたかもしれない。
6 腕 30 分で済んだ。装置を直した投資はここで回収されている。

### 残る候補

複合体メンバーシップ（CORUM）、パラログ / 配列相同性、パスウェイ共属（Reactome）。
（この節で書いた「共必須性と同じ種類の実験だから効いた」という推測について:
§27 で否定したと書いたが、**それは書き過ぎだった**。§27 が否定したのは
「組織発現は共必須性と冗長だから効かない」という**別の説明**であって、
この推測そのものは test していない。**§31 で初めて統制の効いた形で test して、
支持された。**）

## 27. §26 の理由は間違いだった — そして §27 の最初の説明も間違っていた

§26 は「組織発現は共必須性と重なるから足しても薄めるだけ」と結論した。
**重なりを実際に測ったら逆だった。** 本番パネル 300 標的について、
各類似度が選ぶ上位 25 近傍の一致率:

| 組み合わせ | 一致率 |
|---|---|
| STRING vs 共必須性 | 0.191 |
| STRING vs 組織 | 0.110 |
| **共必須性 vs 組織** | **0.081** |

**3 組で最も低い。** 共必須性と組織発現はほとんど別の遺伝子を選んでいる。
冗長どころか**最も相補的**な 2 つだった。§26 の理由は否定された。

### 最初に書いた説明（希釈）も間違いだった

そこで「第 3 ブロックはノルムを奪うので中身に関わらず削る」と書き、
中身をシャッフルしたブロックが実物の 2 倍削る（−0.060 vs −0.032、2 株とも）
ことをその証拠とした。**融合の式を確かめたら、これも違った。**

`blend()` は連結して行ごとに再正規化する。**入れ子にすると重みが等分にならない**:

```
blend(blend(STRING, coess, 1.0), tissue, 1.0)
  → cos = cos_STRING/4 + cos_coess/4 + cos_tissue/2
```

数値でも一致する（STRING +0.0067・共必須性 −0.1368・組織 +0.8888 の対で
融合 cos = +0.4119 = 0.0067/4 − 0.1368/4 + 0.8888/2）。

つまり `--feature-blend 1.0` を 2 回指定したとき、**組織は 3 分の 1 ではなく
類似度の半分を占めていた**。3 つを等分に混ぜる実験は一度も走っていない
（等分にするには第 3 ブロックの重み 0.707）。そしてシャッフル対照の −0.060 は
「第 3 ブロック固有の希釈」ではなく、**類似度の半分をノイズに置き換えた**結果。
当然そうなる。深い発見ではない。

87.4% の遺伝子は 3 ブロックすべてに載っており、その行では融合 cos は
各ブロックの cos の**きれいな重み付き平均**になっている。奪われるノルムはない。

### 実際に効く重みで見ると、単調に「少ないほうがいい」

組織ブロックの実効シェアで並べ直す:

| 指定した重み | 組織の実効シェア | K562 | RPE1 |
|---|---|---|---|
| 1.0 | 50% | −0.0322 | −0.0327 |
| 0.5 | 20% | −0.0177 | −0.0078 |
| 0.25 | 5.9% | −0.0080 | +0.0007 |
| 0.15 | 2.2% | −0.0007 | +0.0062 |

**単調。** K562 はシェアが 0 に近づくほど基準に戻るだけで、一度も超えない。
RPE1 の +0.0062（シェア 2.2%）は片株の山で、§17・§25・§26 で 3 度学んだ形。
等分（33%）は 20% と 50% の間なので、測るまでもなく負。

**§26 の結論（組織発現を足さない）は 2 度理由を書き換えたが、結論自体は動かない。**
組織発現は相補的だが、この課題の `pds` を上げる向きには効かない。

### コードに残した対策

同じ間違いを繰り返さないよう、`blend()` が**各ブロックの実効シェアをログに出す**
ようにした。入れ子の重みは指定した数字と一致しない。

### この 2 度の訂正から

「相補的な情報を足せば良くなるはず」という筋は、**測るたびに違う理由で外れた**。
一致率 0.081 は「別のことを知っている」ことしか意味せず、
**その別のことがこの課題で役に立つ保証はない**。
残る候補（CORUM・パラログ・Reactome）も同じ形で外れる可能性が高い。
次に類似度を試すときは、上乗せではなく**共必須性の置き換え**として
——つまり「共必須性より良いか」を直接——測るほうが情報量が多い。

## 28. 装置の 5 つ目の欠陥 — パネル自身のノイズを測っていなかった

§19 で**リーダーボードの**ノイズ床（Overall ±0.0015）を測ったのに、
**手元のパネルのノイズは一度も測っていなかった。** §24 以降の判定はすべて
シード 0 の 1 回読みで、差が 0.008 でも「2 株で符号が揃った」と書いてきた。
現行構成（LOO・STRING + 共必須性 1.0）で排出シードだけ 0/1/2 と変えて測る:

| | シード 0 | シード 1 | シード 2 | sd | 範囲 |
|---|---|---|---|---|---|
| K562 | +0.1839 | +0.1839 | +0.1660 | 0.0103 | 0.018 |
| RPE1 | +0.1242 | +0.1185 | +0.1650 | **0.0254** | **0.047** |

**RPE1 の絶対値は 1 シードで ±0.025 揺れる。** §24 で「共必須性は RPE1 で +0.0380」
と書いた値と同じ桁。絶対値の 1 回読みはほとんど何も言っていない。

### 対にすれば効くが、それでも ±0.01

同じシードで基準と候補を比べれば共通乱数の効果で揺れは減る。
組織ブロック 0.5（実効シェア 20%）を 3 シードで測った:

| 対の差（候補 − 基準、同一シード） | シード 0 | シード 1 | シード 2 | 平均 | sem | t |
|---|---|---|---|---|---|---|
| K562 | −0.0177 | −0.0358 | −0.0142 | **−0.0226** | 0.0067 | **−3.4** |
| RPE1 | −0.0078 | **+0.0075** | −0.0104 | −0.0036 | 0.0056 | −0.6 |

対にすると sd は 0.010〜0.012 まで下がる（絶対値の 0.010／0.025 から改善）。
それでも **1 シードの差 1 つは ±0.01。3 シードの平均で ±0.006。**

**新しい判定規則: 1 シードで 0.02 未満の差は読まない。3 シードで測って
sem の 2 倍を超えたときだけ本物とする。** 1 腕 5 分なので 1 候補 6 腕 30 分。

### これで読み直す

| 主張 | 元の読み | 3 シードで見ると |
|---|---|---|
| §24 共必須性ブレンド +0.0553 / +0.0380 | 本物 | **本物**（1 シードでも 4〜5σ、しかも本番で +0.0062 が確定済み） |
| §27 シャッフル対照 −0.0607 / −0.0598 | 本物 | **本物**（5σ） |
| §24 LOO 自体 −0.076 / −0.071 | 本物 | **本物** |
| §26 組織 0.5 −0.0177 / −0.0078 | 2 株とも負 | K562 は**本物**（t=−3.4）、**RPE1 はゼロと区別できない**（t=−0.6） |
| §26 組織 0.25・0.15 | 2 株一致せず | **どちらも読めていなかった**（0.008 以下） |
| §25 power 1 +0.0192 / −0.0058 | 符号が反転 | K562 は境界、**RPE1 の −0.0058 はノイズ**。「反転」とまでは言えない |
| §25 STRING 900 −0.0141 / −0.0241 | 2 株とも負 | **1〜2σ。確定とは言えない** |

**結論は 1 つも変わらない**——どれも「採用しない」で、採用には 2 株で
明確な正が要るから。だが**根拠の強さを 3 つ書き過ぎていた**（§25 の STRING 900 を
「確定」、§26 の組織 0.25/0.15 を「単調」、§25 の power 1 を「符号が反転」）。
単調に見えた並びは、±0.01 の揺れの上に乗った並びだった。

### なぜ RPE1 だけ 2 倍揺れるか

RPE1 は平均 `n_real` が 462 で K562 の 254 の 1.8 倍。呼び出し予算は両方 3,000 で
固定なので、RPE1 のほうが「予算に対して本物の応答が多い」領域にいる。
`pds` は順位の平均なので、順位が入れ替わりやすい領域ほど揺れる。
**本番の平均 `n_real` は約 275 で K562 に近い**（`docs/06`）ので、
2 株の一致を求めるとき RPE1 の 1 回読みに重みを置きすぎるのは筋が悪い。

## 29. 細胞数は水準を動かすが、比較は動かさない — 100 細胞のままでよい

§28 で RPE1 の揺れが K562 の 2 倍だと分かったので、原因が排出の細胞数だと考えて
**本番と同じ 400 細胞/摂動**で測り直した（現行は 100）。RPE1・現行構成・3 シード:

| | シード 0 | 1 | 2 | 平均 | sd |
|---|---|---|---|---|---|
| 100 細胞 | +0.1242 | +0.1185 | +0.1650 | +0.1359 | 0.0254 |
| **400 細胞** | +0.1866 | +0.2180 | +0.1551 | **+0.1866** | 0.0315 |

**揺れは減らなかった。** sd は 0.0254 → 0.0315 だが、**3 シードの sd 推定同士の差は
読めない**（n=3 の sd 推定の不確かさは 50% 程度）。§28 の規則を分散にも適用すると、
「400 のほうが騒がしい」とは言えない。**言えるのは水準が +0.0507 上がったこと。**

### 既知の実効果で答え合わせ

水準が動くこと自体は、比較しか使わないなら害がない——**差が保たれるなら**。
本番で +0.0062 が確定している共必須性ブレンドの利得を、400 細胞・3 シードで測る
（STRING のみ 400 細胞は +0.1437 / +0.1635 / +0.1238）:

| | 共必須性の利得（同一シードの対） | 平均 | t |
|---|---|---|---|
| 400 細胞 | +0.0429 / +0.0545 / +0.0313 | **+0.0429 ± 0.0067** | **+6.4** |
| 100 細胞（シード 0 のみ） | +0.0380 | — | — |

**一致する。** 100 細胞のパネルは実効果をそのまま映していた。
しかも対の sd は 400 細胞で 0.0116、100 細胞の組織比較で 0.0097 と**変わらない**。

### 結論: 100 細胞のままでよい

細胞を 4 倍にしても対のノイズは下がらない。**±0.01 の揺れは排出のサンプリング由来ではない**
（そうなら √4 = 2 倍静かになるはず）。順位の平均という `pds` の形そのものから来ている。
だから **1 腕 15 分の 400 細胞に移る理由はなく、5 分の 100 細胞のままでよい。
ノイズを下げる手段はシードを増やすことだけ**——§28 の規則が唯一の対処法だと確定した。

副産物として **§24 の RPE1 の利得がちゃんと測れた**: シード 0 の 1 回読み +0.0380 が、
3 シード対で **+0.0429 ± 0.0067（t=6.4）**。共必須性ブレンドは手元でも 6σ で本物。

## 30. `shared_shrink` を直した装置で振り直した — 現行 1.0 が頂点だった

`shared_shrink`（全 target の平均を各予測から引く）は v11 → v12 を倍増させたつまみで、
現行 1.0。決めた根拠（`docs/07` §10b・§16c）は**希釈された装置・K562 のみ・1 シード**で、
頂点 0.75 と 1.0 の差は scaled +0.003 ＝ **§28 の規則では読めない値**だった。

再掃引する理由もあった。本番は 300 標的すべてが kNN 平均なので（§23）、
予測どうしが希釈された領域より似やすい。`pds` は「予測どうしがどれだけ違うか」を
読む指標なので、**最適な引き算の量が動いていておかしくない**。

直した装置（LOO・両株・3 シード・同一シードで対）で振り直した:

| `shared_shrink` | K562 | RPE1 | 判定 |
|---|---|---|---|
| 0.5 | −0.0158（t=−1.3） | **−0.0103（t=−6.6）** | 悪い |
| 0.75（**旧掃引の頂点**） | **−0.0059（t=−2.2）** | **−0.0049（t=−3.2）** | 悪い |
| **1.0（現行）** | **基準** | **基準** | **頂点** |
| 1.5 | **−0.0219（t=−4.0）** | +0.0016（t=+0.7、ゼロと区別できない） | 悪い |

**両側とも悪い。1.0 が頂点。** そして**旧掃引が選んでいた 0.75 は、
直した装置では 2 株とも明確に悪い**（t=−2.2・−3.2）。希釈された装置は
ここでも逆を指していた（§25 の低ランク成分、§26 の組織発現に続いて 3 度目）。

**現行値を変える理由はない。** つまみを回して得るものはこれで尽きた。

### 副産物: 近い設定どうしなら装置はもっと細かく読める

§28 は対の差の sd を 0.010〜0.012 と測ったが、それは**組織ブロックを足す**という
大きな変更での話だった。`shared_shrink` を 1.0 → 0.75 に動かすような**小さな変更**では
対の sd は 0.0027〜0.0045 まで落ちる。共通乱数が効くのは、予測が近いときだけ。

| 比べたもの | 対の差の sd |
|---|---|
| 組織ブロックを足す（§28） | 0.0097〜0.0116 |
| shrink 0.5 対 1.0 | 0.0027（RPE1）〜0.0206（K562） |
| **shrink 0.75 対 1.0** | **0.0027〜0.0045** |

**§28 の「1 シードで 0.02 未満は読まない」は大きな変更についての規則**で、
つまみを少し動かすだけなら 3 シードで 0.005 の差も読める（t=−3.2 が出ている）。
候補の性質で使い分けること。

### 走らせ方の注意

**このコンテナは数時間おきに再起動し、走っているジョブを殺す**（8/30 は 05:00Z と
09:35Z の 2 回）。掃引は `$SP/sweep_resumable.sh` の形——**1 腕ごとに
`$SP/results/<株>_shrink<値>_seed<n>.txt` に書き、既にあれば飛ばす**——で回すこと。
再起動しても失うのは走っていた 1 腕だけで、同じコマンドで再開できる。

## 31. CCLE 発現 — 「同じ種類の実験だから効く」の統制された test

§26 で「共必須性が効いたのは**この課題と同じ種類の実験**（CRISPR ノックアウトの
表現型）だからかもしれない」と書き、§27 で軽率に「否定した」と書いた。
**§27 が否定したのは冗長性の説明であって、この推測ではない。**
今回それを**統制の効いた形**で test した。

DepMap は同じ細胞株パネルについて**発現量**も公開している
（`OmicsExpressionProteinCodingGenesTPMLogp1.csv`、18,383 遺伝子 × 1,673 株）。
共必須性（1,178 株のノックアウト後の増殖）とほぼ同じ株、ほぼ同じ次元数で、
**違うのは「何を測ったか」だけ** —— 定常状態の発現か、遺伝子を潰した後の表現型か。
カバー率は 4 つの類似度で最良（本番パネル 300 標的の 299）。
近傍の一致率は共必須性と 0.080、組織版とも 0.120 で、**どちらとも別物**。

### 共必須性の置き換えとして（3 シード・両株・同一シードで対）

| | K562 | RPE1 |
|---|---|---|
| CCLE 発現に置き換え | **−0.0562（t=−10.5）** | **−0.0118（t=−4.4）** |

**2 株とも明確に悪い。** 現行の共必須性が勝つ。

### 上乗せとして（実効シェア 20%）

| | K562 | RPE1 |
|---|---|---|
| 現行 ＋ CCLE 発現 | **−0.0357（t=−6.2）** | **+0.0092（t=+2.3）** |

**符号が逆で、どちらも有意。** §17・§25 で 2 度見た「片株の山」。却下。

### 統制された比較 — 測り方が効いている

STRING のみを基準に、3 つの類似度を足したときの利得（シード 0）:

| 足した類似度 | 何を測ったか | 次元 | K562 | RPE1 |
|---|---|---|---|---|
| **DepMap 共必須性** | **遺伝子を潰した後の増殖** | 1,178 株 | **+0.0553** | **+0.0380** |
| CCLE 発現 | 定常状態の発現 | 1,673 株 | +0.0098 | +0.0310 |
| GTEx 組織発現 | 定常状態の発現 | 68 組織 | +0.0338 | +0.0035 |

**2 株とも勝つのは共必須性だけ。** 発現由来の 2 つはどちらも片株でしか勝たない。
そして CCLE は共必須性と**ほぼ同じ株・ほぼ同じ次元**なので、
この差は「データ量」でも「どの細胞株か」でもなく、**測り方そのもの**から来ている。

**§26 の推測は支持された。** 効く類似度は「摂動の表現型」であって、
「遺伝子が一緒に動くか」ではない。次元を 68 → 1,673 と 25 倍にしても
発現は発現のままで、共必須性に届かない。

### 残る候補の期待値は下がった

CORUM（複合体）・Reactome（パスウェイ）・パラログはいずれも**注釈由来**で、
「摂動の表現型」からは最も遠い。STRING がすでに同種の証拠を含んでいることもあり、
**期待値は低い**と見るべき。効く可能性があるのは、別の摂動スクリーンから来る
類似度——Sanger Project Score（独立した CRISPR 増殖スクリーン）、
薬剤感受性プロファイル（PRISM）、あるいは形態プロファイル（Cell Painting）——のほうだが、
どれも DepMap の共必須性と強く相関する可能性が高い。

**類似度の側で残っている見込みは薄い。** つまみ（§25・§30）に続いて、
この線もほぼ尽きた。

## 32. リーダーボードを読んだ — 12 回分の前提が間違っていた

`https://virtualcellchallenge.org/api/leaderboard` は**メンバー別スコア・モデル名・
説明文**を全部返す。484 件を落として読んだ。**自分は 203 位。**

### 生値で見た自分の位置

| | `pds` | `reach` | `jac` | `fid` |
|---|---|---|---|---|
| 1 位 GeroAI_v12 | 0.8199 | 0.2621 | 0.0288 | 0.5134 |
| 2 位 **STATE baseline**（Arc 公式） | 0.8186 | 0.2132 | 0.0318 | 0.5089 |
| 「toy model」 | 0.8481 | 0.2725 | 0.0285 | 0.5096 |
| **自分（v20）** | **0.5807** | **0.0974** | 0.0299 | 0.5048 |
| ランダム | 0.50 | — | — | — |

`jac`・`fid` だけ互角で、点の大半を持つ `pds`・`reach` は**チャンスに近い側**。

### 予算付き呼び出し集合の前提が崩れた

この設計の全体は「宣言数を絞れば `jac`/`fid` が守れる」というトレードオフに乗っていた。
484 件で相関を取ると**逆**だった:

| | 相関 |
|---|---|
| 生 `pds` と 生 `jac` | **+0.410** |
| 生 `pds` と 生 `reach` | **+0.677** |

**トレードオフは存在しない。** 自分の `jac` 以上を取りながら `pds` も高い相手が 134 件いる。

### そして本当の原因 — `essential` サブセットを使っていた

説明文に「Replogle K562 **gwps**」「public K562 **genome-wide** CRISPRi」が並び、
ある投稿は coverage を明記していた: 「Replogle K562 gwps log1p-delta transfer **(272/300)**」。

手元の Replogle は K562 2,058 摂動・RPE1 2,394 摂動。これは scPerturb の
`ReplogleWeissman2022_K562_essential.h5ad`（1.55 GB、手元のサイズと完全一致）。
同じ Zenodo に **`K562_gwps.h5ad`（9,867 摂動）** がある。**自分で確かめた:**

| ライブラリ | 本番 300 標的のカバー率 |
|---|---|
| いまの essential 版 | **25/300**（うち K562・RPE1 は 0、H1 が 25） |
| **genome-wide 版** | **272/300（91%）** |

`docs/06` の「2026 パネルの 300 遺伝子は Replogle と 1 つも重ならない」は
**essential 版に限った話**だった。チャレンジは非必須遺伝子を選んでおり、
必須遺伝子スクリーンに無いのは当然。**そこから派生した推論が全部倒れる:**

* **§23 の「本番は 300 標的すべてが kNN 腕」** — 誤り。本番は 267/300 が D 腕。
  したがって §24 で入れた `--holdout-target-signatures` は、**本番を模していない**。
  あれは「本番 0/300」という誤った前提の下でのみ正しかった。
* **§17・§25 の「オラクル類似度でも 0.748 が天井」** — 上位陣は 0.82。
  類似度を磨いて越えられる壁ではなかった。越える必要がなかった。
* **§26〜§31 で潰した候補**（組織発現・CCLE 発現・CORUM 等）は
  **外挿の精度を上げる作業**だった。上位陣は外挿していない。

**単一のデータファイルの取り違えが、12 回の提出と 15 節分の解析の前提だった。**

## 33. 直した — パネルで +0.1038（t=+18.2）

`scripts/build_library_gwps.py` を足した。単一細胞版 8.8 GB は不要で、
Replogle は**摂動ごとの疑似バルク**（`K562_gwps_raw_bulk_01.h5ad`、375 MB）も
公開している。それは `SignatureLibrary` が持つものそのもの。
z 正規化版ではなく**生カウント版**を使う（正規化版は対照に対する z で、
対照の分散が 0 の遺伝子に inf が入る。生カウントなら既存と同じ normlog に通せる）。

RPE1 パネル・3 シード・同一シードで対（本番と同じ構図で比較）:

| 構成 | シード 0 | 1 | 2 |
|---|---|---|---|
| **v19 相当**（essential・holdout・予算 3,000） | +0.1242 | +0.1185 | +0.1650 |
| GWPS 全 9,743 + 予算 3,000 | +0.1968 | +0.1799 | +0.2016 |
| GWPS 全 9,743 + 予算 10,000 | +0.2181 | +0.1875 | +0.2168 |
| **GWPS を 300 標的に限定 + 予算 10,000** | **+0.2386** | **+0.2205** | **+0.2599** |

| 差 | 平均 | t |
|---|---|---|
| ライブラリだけ差し替え（予算 3,000 のまま） | +0.0569 | +5.3 |
| その上で予算 3,000 → 10,000 | +0.0147 | +3.7 |
| **合計（限定版）** | **+0.1038** | **+18.2** |
| 限定版 対 全件版 | +0.0322 | +4.9 |

**共必須性ブレンド（パネル +0.043 → 本番 Overall +0.0062）の 2.4 倍**の大きさ。

### 2 つの副次的な発見

**予算は「無害」ではなく「信号が無いときだけ無害」だった。** essential 版で
予算を 3,000 → 20,000 にしても `pds` は 0.5865 → 0.5816 で動かなかった。
中身が外挿＝ほぼノイズだったので、形を潰しても失うものが無かった。
**実測シグネチャが入った途端に予算が効き始める**（+0.0147、t=3.7）。

**GWPS は全部入れるより 300 標的に限定するほうが良い**（+0.0322、t=4.9）。
9,400 件の無関係なシグネチャは近傍プールを薄めるだけで、D 腕には寄与しない。
しかも全件だと (標的 × 遺伝子) の累積行列が 15 GB のメモリに収まらない。

## 34. 13 回目の提出（v23）— ライブラリは効いた、予算が壊した

GWPS 267 標的 + 予算 10,000。**Overall +0.019155（217 位）で v19 の +0.031098 より下**。
だが中身は正反対の 2 つに分かれる:

| 指標 | v19 | v23 | 差 |
|---|---|---|---|
| **`pds`** | +0.1946 | **+0.2239** | **+0.0293** |
| `nmae` | +0.0022 | **−0.1020** | **−0.1042** |
| `fid` | −0.0266 | −0.0186 | +0.0080 |
| `reach` | +0.0178 | +0.0124 | −0.0054 |
| `jac` | −0.0014 | −0.0009 | +0.0005 |
| Overall | +0.0311 | +0.0192 | −0.0119 |

**genome-wide ライブラリは狙いどおり効いた。** `pds` +0.0293 は §19 のノイズ床 ±0.0024 の
12 倍。パネルの +0.1038 からの換算率は 0.28 で、§24 で 1 点だけ得ていた 0.42 と同じ桁。
**§32・§33 の本筋は正しかった。**

**壊したのは予算 3,000 → 10,000 のほう。** `nmae` が単独で −0.1042。
ビルドログが理由を言っている: **1 標的あたり 9,599 遺伝子を median |lfc| 1.343 で動かした**
（v19 は 3,000 遺伝子・呼び出しの大きさの中央値 0.255 log2）。
真値がほぼ 0 の遺伝子に大きな fold change を割り当てれば log-FC の絶対誤差は壊れる。

### §32 のリーダーボード分析は片手落ちだった

§32 で「`pds` と `jac`（+0.410）・`reach`（+0.677）は正相関だからトレードオフは無い」と
結論したが、**`nmae` を見ていなかった**。484 件で測り直すと:

| | 相関 |
|---|---|
| 生 `nmae` と 生 `pds` | **−0.241** |
| 生 `nmae` と 生 `jac` | +0.198 |
| 生 `nmae` と 生 `reach` | +0.063 |

（生 `nmae` は小さいほど良い指標なので、`pds` との負の相関は「pds が高い提出ほど
nmae も良い」を意味する。つまり**上位陣は両立させている**。）

**トレードオフは存在した。相手を取り違えていた** —— 予算が守っていたのは
`jac`/`fid` ではなく **`nmae`** だった。§32 の「絞る理由が無かった」は
`jac`/`fid` については正しく、`nmae` については誤り。

### 次

v24 = 同じライブラリ + **予算 3,000 に戻す**。`pds` の利得を残して `nmae` を守る。
その先の本命は、宣言した遺伝子に**閾値通過の大きさではなくモデルの予測倍率**を出すこと。
実測シグネチャが入った今は倍率自体に意味があり、`nmae` と `pds` の両方に効くはず。
ただし `nmae` を手元で測る装置（H1 排出）が要る。

## 35. 14 回目の提出（v24）— 自己ベスト更新 +0.035923

同じ GWPS ライブラリ、予算だけ 10,000 → 3,000（v19 と同じ）に戻した。

| 指標 | v19 | v23（予算 10,000） | **v24（予算 3,000）** | v24 − v19 |
|---|---|---|---|---|
| `pds` | +0.1946 | +0.2239 | **+0.2154** | **+0.0208** |
| `nmae` | +0.0022 | −0.1020 | **+0.0103** | +0.0081 |
| `fid` | −0.0266 | −0.0186 | −0.0240 | +0.0026 |
| `reach` | +0.0178 | +0.0124 | +0.0156 | −0.0022 |
| `jac` | −0.0014 | −0.0009 | −0.0017 | −0.0003 |
| **Overall** | +0.031098 | +0.019155 | **+0.035923** | **+0.0048** |

**診断どおりだった。** 予算を戻すと `nmae` は −0.1020 → +0.0103 に完全復帰し、
`pds` は +0.2239 → +0.2154 と少しだけ落ちた（予算を広げた分の +0.0085 を返した）。
差し引き **Overall +0.035923 で自己ベスト**（従来 v19 の +0.031098）。

### ライブラリの寄与が確定した

v19 → v24 は**ライブラリの差し替えだけ**（essential → GWPS 267 標的）。
**`pds` +0.0208、Overall +0.0048**、ノイズ床 ±0.0015 の 3.2 倍。
共必須性ブレンド（+0.0062）に次ぐ 2 番目に大きい単一の改善。

パネルは同じ差し替えを +0.0569 と読んでいた（§33）。換算率 0.37 で、
§24 の 0.42、v23 の 0.28 と同じ桁。**直したパネルの換算率はこれで 3 点になった**
（0.42 / 0.28 / 0.37、平均 0.36）。タスク #10 はこれで閉じる。

### まだ届いていない

上位陣は `pds` 生値 0.82、自分は 0.59。**GWPS を入れてなお 0.23 の差**がある。
267/300 が実測シグネチャを持つのに届かないということは、
**持っている実測をうまく使えていない**ということ。次の 2 つが本命:

1. **宣言した遺伝子に閾値通過の大きさではなくモデルの予測倍率を出す。**
   いまは `1.314 × cpm^-0.456` を `call_size_gamma` で傾斜させた値で上書きしている。
   essential 版＝ほぼノイズの時代には妥当だったが、実測が入った今は倍率自体に情報がある。
2. **予算 K の測り直し。** 3,000 と 10,000 の 2 点しか無く、しかも 10,000 は
   `nmae` を壊した。最適は 3,000 の近傍か、それより下かもしれない。
   **`nmae` を手元で測る装置（H1 排出）が要る。**

## 36. `nmae` の装置を選び直した — RPE1 パネルは逆を指す

§35 の次の一手（予算の測り直しと予測倍率の受け渡し）はどちらも `nmae` を手元で
測る必要がある。H1 の参照はディスク確保のため消してしまっていたので、
**まず手近な K562/RPE1 パネルで `--skip-de` を外せば済むのでは**と考えて測った。

RPE1 パネル・v24 構成・シード 0:

| 予算 | `pds` | `fid` | `jac` | **`nmae`**（小さいほど良い） |
|---|---|---|---|---|
| 3,000 | 0.5783 | 0.3469 | 0.0155 | **0.9776** |
| 10,000 | 0.5843 | 0.4853 | 0.0411 | **0.9629** |

**RPE1 は「10,000 のほうが良い」と言う。本番は逆だった**（v23 で `nmae` −0.1042）。
`pds` も RPE1 では 10,000 が上（0.5843 対 0.5783）で、これも本番と逆。
**RPE1 パネルを `nmae` の装置に使ってはいけない。** 危うく 6 つ目の装置の欠陥を踏むところだった。

理由は §29 で見た領域の違い。RPE1 の平均 `n_real` は 462、本番は約 275。
予算に対して本物の応答が多い株では、宣言を増やすほど真陽性を拾えてしまう。

### H1 を復旧した — 15 GB は要らなかった

`docs/06` の手順は 2025 の**訓練**分割（15.5 GB）を落とすが、
**検証分割（6.9 GB）で足りる**。2 分で落ちて 45 摂動の参照ができる。
ただし残っていた DE 真値 `de_ref_h1_s60.parquet` は訓練分割の 60 摂動のもので、
**標的が 1 つも重ならない**。`scripts/local_reference_de.py` で新しい参照の
真値を計算し直せば済む（7 分）。

復旧した装置は本番に近い: **`n_real` の中央値 203**（本番 ≈275、RPE1 は 462）。

### 予算の頂点は 3,000 のままだった（H1・`pds`）

| 予算 | 1,500 | **3,000** | 10,000 |
|---|---|---|---|
| `pds` scaled | +0.1526 | **+0.2824** | +0.1845 |

**両側が悪い。** 本番（v24 の 3,000 が v23 の 10,000 に勝った）と符号が一致する。
§35 で「最適は 3,000 の近傍か下かもしれない」と書いたが、**下も悪い**。
現行値を動かす理由はない。

v24 構成の H1 での全メンバー: `fid` 0.4894・`reach` 0.1189・`jac` 0.0734・
`nmae` 0.9928、`sum_scaled/6` = 0.0180。

### 直したバグ

`tables_to_arrays` は共通標的が 0 のとき `np.array([])` が float になり、
3 フレーム下で `IndexError` を出していた。何が悪いか分からない。
**「共通する標的が無い。参照に合わせて DE 真値を計算し直せ」と言うようにした。**

## 37. 予測倍率を出す — 却下

§35 の本命。宣言した遺伝子の大きさは `significance_magnitude`（発現量だけの関数）に
ランプと γ 傾斜を掛けたもので、**モデルの予測倍率は符号と順位にしか使われていない**。
essential 版＝外挿だった時代には妥当だったが、実測シグネチャが入った今は倍率に情報があるはず。

`predicted_magnitude`（0 で現行、1 でモデルの倍率）を足して H1 で測った。
v24 構成（GWPS300・予算 3,000・シード 0）:

| 構成 | `pds` | `fid` | `reach` | `jac` | `nmae` | **`sum_scaled/6`** |
|---|---|---|---|---|---|---|
| **w=0（現行）** | 0.6327 | 0.4894 | 0.1189 | 0.0734 | 0.9928 | **+0.0180** |
| w=0.5 | 0.6312 | 0.4893 | 0.1193 | 0.0734 | 0.9928 | +0.0180 |
| w=1.0 | 0.6307 | 0.4893 | 0.1162 | 0.0734 | 0.9930 | +0.0173 |

**何も動かない。** 理由は実装を読めば分かる: 予測倍率は有意性しきい値で下限を切っており、
**モデルの倍率はほぼ常にしきい値より小さい**ので、下限が毎回勝つ。

### 下限を外すと壊れる

`--no-magnitude-floor` を足して、正直な（小さい）倍率をそのまま出す:

| 構成 | `pds` | `fid` | `reach` | `jac` | `nmae` | **`sum_scaled/6`** |
|---|---|---|---|---|---|---|
| w=0（現行） | 0.6327 | 0.4894 | 0.1189 | 0.0734 | 0.9928 | **+0.0180** |
| w=0.5・下限なし | 0.6147 | 0.4295 | 0.1142 | 0.0490 | **0.9748** | −0.0222 |
| w=1.0・下限なし | 0.6095 | 0.2328 | 0.0814 | 0.0151 | 0.9902 | **−0.1571** |

**`nmae` は確かに動く**（0.9928 → 0.9748 が最良）が、`jac` が 0.0734 → 0.0490、
`fid` が 0.4894 → 0.4295 と落ちて**差し引き大きく負**。
w=1.0 では `nmae` すら悪化する（0.9902）——倍率が小さすぎて今度は過小評価になる。

**この線は閉じる。** 呼び出しを有意にするには発現量が決めるしきい値を超える必要があり、
モデルの倍率はそこに届かない。**排出の大きさをどういじっても `nmae` は 0.99 から動かない。**

### 意味するところ

上位陣の生 `nmae` は 0.889〜0.922、こちらは 0.993。**この差は排出の方針では埋まらない。**
「どの遺伝子を呼ぶか」と「その delta がどれだけ正確か」——つまりモデルの質そのもの。
`pds` の 0.59 対 0.82 と同じ場所を指している。

つまみ側で残っているものは無い。次に効くとすれば**転移の作り方**（いまは
K562 の実測を fold change としてリベースし、近傍平均と混ぜるだけ）であって、
その出し方ではない。
